import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  text: {
    textAlign: 'right',
    marginRight: 8,
  },
  image: {
    width: 40,
    height: 40,
  },
});

export default styles;
